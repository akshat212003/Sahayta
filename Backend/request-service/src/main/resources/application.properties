spring.application.name=request-service

server.port=8083

# MySQL Configuration (Supports Environment Variables with Local Defaults)
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/request_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:7760}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Eureka Server
eureka.client.service-url.defaultZone=http://${EUREKA_HOST:localhost}:8761/eureka

jwt.secret=bXlzZWNyZXRrZXlteXNlY3JldGtleW15c2VjcmV0a2V5MTIzNDU2Nzg5MDEyMw==

# Donation Service URL
donation.service.url=http://${DONATION_HOST:localhost}:8082